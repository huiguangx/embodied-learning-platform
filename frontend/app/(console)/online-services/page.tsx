"use client";import{useEffect,useState}from"react";import{Shell}from"../../../components/shell";import{DataTable}from"../../../components/data-table";import{api}from"../../../lib/api";
export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api<{items:any[]}>('/online-services').then(x=>setD(x.items)).catch(()=>{})},[]);return <Shell><div className="page"><h1>Online Services</h1><p className="muted">Local serving adapter</p><DataTable headers={["Service","Model","Status","Endpoint","Policy"]} rows={d.map(x=>[x.name,x.modelVersionId,x.status,x.endpoint,x.versionPolicy])}/></div></Shell>}
