"use client";import{useEffect,useState}from"react";import{Shell}from"../../../components/shell";import{DataTable}from"../../../components/data-table";import{api}from"../../../lib/api";
export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api<{items:any[]}>('/resource-groups').then(x=>setD(x.items)).catch(()=>{})},[]);return <Shell><div className="page"><h1>Resource Groups</h1><p className="muted">Quota and cluster access</p><DataTable headers={["Name","Node type","GPU quota","CPU quota","Concurrency"]} rows={d.map(x=>[x.name,x.nodeType||'GPU',x.gpuQuota,x.cpuQuota,x.concurrency])}/></div></Shell>}
