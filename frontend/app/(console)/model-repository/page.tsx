"use client";import{useEffect,useState}from"react";import{Shell}from"../../../components/shell";import{DataTable}from"../../../components/data-table";import{api}from"../../../lib/api";
export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api<{items:any[]}>('/model-versions').then(x=>setD(x.items)).catch(()=>{})},[]);return <Shell><div className="page"><h1>Model Repository</h1><p className="muted">Registered model versions and provenance</p><DataTable headers={["Model","Experiment","Version","Format","Approval"]} rows={d.map(x=>[x.id,x.experimentRunId,x.version,x.format,x.approvalStatus])}/></div></Shell>}
