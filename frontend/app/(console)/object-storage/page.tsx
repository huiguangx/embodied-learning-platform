"use client";import{useEffect,useState}from"react";import{Shell}from"../../../components/shell";import{DataTable}from"../../../components/data-table";import{api}from"../../../lib/api";
export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api<{items:any[]}>('/assets/objects?bucket=ai-interaction').then(x=>setD(x.items)).catch(()=>{})},[]);return <Shell><div className="page"><h1>Object Storage</h1><p className="muted">Bucket: ai-interaction /</p><DataTable headers={["Name","Bucket","Prefix","Size","Storage"]} rows={d.map(x=>[x.name,x.bucket,x.prefix,x.size,x.storageType])}/></div></Shell>}
