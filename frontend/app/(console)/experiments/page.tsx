"use client";import{useEffect,useState}from"react";import{Shell}from"../../../components/shell";import{DataTable}from"../../../components/data-table";import{api}from"../../../lib/api";
export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api<{items:any[]}>('/experiment-runs').then(x=>setD(x.items)).catch(()=>{})},[]);return <Shell><div className="page"><h1>Experiments</h1><p className="muted">Runs, metrics and artifacts</p><DataTable headers={["Run","Training job","Best","Artifacts"]} rows={d.map(x=>[x.id,x.trainingJobId,x.best?'Yes':'No',x.artifactsUri])}/></div></Shell>}
