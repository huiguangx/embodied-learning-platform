"use client";import{useEffect,useState}from"react";import{Shell}from"../../../components/shell";import{DataTable}from"../../../components/data-table";import{api}from"../../../lib/api";
export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api<{items:any[]}>('/assets/images').then(x=>setD(x.items)).catch(()=>{})},[]);return <Shell><div className="page"><h1>Image Repository</h1><p className="muted">Container images and immutable digests</p><DataTable headers={["Name","Namespace","Access","Digest","Updated"]} rows={d.map(x=>[x.name,x.namespace,x.accessLevel,x.digest,x.updatedAt])}/></div></Shell>}
