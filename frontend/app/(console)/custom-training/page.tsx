"use client";import {useEffect,useState} from "react";import {Shell} from "../../../components/shell";import {DataTable} from "../../../components/data-table";import {StatusBadge} from "../../../components/status-badge";import {api} from "../../../lib/api";import type {Job} from "../../../lib/types";
export default function Page(){const [jobs,setJobs]=useState<Job[]>([]);const [error,setError]=useState("");useEffect(()=>{api<{items:Job[]}>('/training-jobs').then(x=>setJobs(x.items)).catch(e=>setError(e.message))},[]);return <Shell><div className="page"><div className="page-title"><div><h1>Custom Training</h1><p className="muted">Training jobs and lifecycle</p></div><button disabled>Create training job</button></div>{error&&<div className="error">{error}</div>}<DataTable headers={["Task","Status","Queue","Image","Code","Dataset","Created"]} rows={jobs.map(j=>[j.name,<StatusBadge key={j.id} value={j.state}/>,j.queueId,j.imageDigest,j.codeVersion,j.datasetVersion,j.createdAt??'-'])}/></div></Shell>}
